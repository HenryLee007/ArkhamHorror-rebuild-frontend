name: "[Frontend] Deploy to Cloudflare Pages"

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["[Backend] Deploy"]
    branches: [main]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - env:
          CF_DEPLOY_HOOK: ${{ secrets.CF_DEPLOY_HOOK }}
        run: |
          curl -X POST "$CF_DEPLOY_HOOK"
